<div class="limited-display">

  <div class="left">
    <%= link_to image_tag(item.main_image ? item.main_image.preview_image : "no_image_preview.gif"), shortcut_path(item.node.first.shortcut) %>
  </div>

  <div class="right">
    <h3><%= item.name %></h3>
    <p><%= number_to_currency(item.cost) %></p>
  </div>

</div>