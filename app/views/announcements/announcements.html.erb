<table>
  <tr>
    <th>Title</th>
    <th>Date</th>
  </tr>
  <% @pages.each do |page| %>
    <tr>
      <%= render 'partials/page_list', :locals => {:page => page} %>
      <td><%= link_to "Read More", announcement_path(page.id) %></td>
    </tr>
  <% end %>
</table>
<%= link_to "Home", home_path %>

